# worker-media

Standalone NestJS worker для FFmpeg, создания HLS, превью и другой обработки медиа.
Он не поднимает HTTP-сервер и не владеет пользовательскими API.

```bash
pnpm nx serve worker-media
pnpm nx test worker-media
pnpm nx build worker-media
```
